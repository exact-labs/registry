[tasks]
clean = { info = "remove binary", path = "", script = "rm bin" }
build = { info = "create binary", path = "", script = ["go build .", "mv just debug/bin"] }
debug = { info = "test binary", path = "debug", script = "{{CWD}}/debug/bin serve --debug" }
